# Pinned runtime dependencies (mirror of pyproject.toml [project].dependencies).
# Install with:  python -m pip install -r backend/requirements.txt
# torch is CPU-only by default; for CUDA see https://pytorch.org/get-started/locally/
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
websockets==14.1
numpy==2.1.3
pandas==2.2.3
PyYAML==6.0.2
python-dotenv==1.0.1
SQLAlchemy==2.0.36
torch==2.5.1
scipy==1.14.1

# dev
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
